cache:
  paths:
    - node_modules/

stages:
  - test
  - build
  - deploy

unit_test:
  stage: test
  script:
    - deploy/unit_test.sh

build_app:
  stage: build
  script:
    - deploy/build_app.sh

build_docker_image:
  stage: build
  script:
    - deploy/build_docker_image.sh