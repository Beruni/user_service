{
  "name": "user_service",
  "version": "1.0.0",
  "description": "User service for Beruni.",
  "main": "app.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build:dir": "mkdir -p ./dist/js",
    "test": "echo '=> running tests' && ./node_modules/.bin/jasmine-node spec && echo '=> tests passed'",
    "minify:js": "echo '=> minify:js' && uglifyjs ./*.js -o ./dist/js/app.min.js --source-map ./dist/js/app.js.map && echo '=> minify:js completed'",
    "start": "node ./dist/js/app.min.js",
    "build": "echo 'Running build....' && npm run clean && tsc && npm run build:dir && npm run test && npm run minify:js",
    "tsc": "tsc",
    "tsc:w": "tsc -w",
    "lite": "lite-server",
    "typings": "typings",
    "postinstall": "typings install"
  },
  "repository": {
    "type": "git",
    "url": "git@git.thoughtworks.net:beruni/user_service.git"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.14.0",
    "mongodb": "^2.1.18"
  },
  "devDependencies": {
    "jasmine-node": "^1.14.5",
    "source-map-loader": "^0.1.5",
    "ts-loader": "^0.8.2",
    "uglify-js": "^2.7.0",
    "typings":"1.3.1"
  }
}
